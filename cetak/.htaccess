<IfModule mod_rewrite.c>
RewriteEngine On

# Allow access to cetak PHP files (they have their own cookie authentication)
# Block only function.php which should not be accessed directly
RewriteCond %{REQUEST_FILENAME} function\.php$
RewriteRule ^(.*)$ - [F,L]
</IfModule>

# Disable directory listing
Options -Indexes
